"use strict";
// T-Shirt: write a function called make shirt() that accepts a size and the text of a message
// that should be printed on the shirt. The function should print a sentence summarizing the size of the 
//shirt and the message printed on it. Call the function.
function makeShirt(size, text) {
    console.log(`Creating a ${size} shirt with the message: ${text} `);
}
makeShirt('large', 'hello world');
makeShirt('medium', 'Hi cutie');
makeShirt('small', 'never give up ');
